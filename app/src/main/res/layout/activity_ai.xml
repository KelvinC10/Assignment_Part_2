package my.edu.utar.assignmentpart2;

    import android.content.Intent;
    import android.os.Bundle;
    import android.view.View;
    import android.widget.Button;

    import androidx.activity.EdgeToEdge;
    import androidx.appcompat.app.AppCompatActivity;
    import androidx.cardview.widget.CardView;
    import androidx.core.graphics.Insets;
    import androidx.core.view.ViewCompat;
    import androidx.core.view.WindowInsetsCompat;

    import com.google.android.material.bottomnavigation.BottomNavigationView;

    public class AI extends AppCompatActivity {

    private Button chatbotBtn;
    private CardView chatbotCard;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    EdgeToEdge.enable(this);
    setContentView(R.layout.activity_ai);

    // Apply window insets for edge-to-edge support
    ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
    Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
    v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
    return insets;
    });

    // Initialize UI components
    chatbotBtn = findViewById(R.id.chatbotBtn);
    chatbotCard = findViewById(R.id.chatbotCard);

    // Create a common click listener for both the button and the card
    View.OnClickListener startChatListener = v -> {
    Intent intent = new Intent(AI.this, ChatActivity.class);
    startActivity(intent);
    };

    chatbotBtn.setOnClickListener(startChatListener);
    if (chatbotCard != null) {
    chatbotCard.setOnClickListener(startChatListener);
    }

    // Setup Bottom Navigation
    setupNavigation();
    }

    private void setupNavigation() {
    BottomNavigationView bottomNavigationView = findViewById(R.id.bottomNavigation);
    bottomNavigationView.setSelectedItemId(R.id.nav_AI);

    bottomNavigationView.setOnItemSelectedListener(item -> {
    int id = item.getItemId();

    // Use a Class variable to determine the next activity
    Class<?> targetActivity = null;

    if (id == R.id.nav_home) {
    targetActivity = MainActivity.class;
    } else if (id == R.id.nav_location) {
    targetActivity = Location.class;
    } else if (id == R.id.nav_food) {
    targetActivity = Food.class;
    } else if (id == R.id.nav_more) {
    targetActivity = More.class;
    } else if (id == R.id.nav_AI) {
    // Already on this page, do nothing
    return true;
    }

    if (targetActivity != null) {
    Intent intent = new Intent(AI.this, targetActivity);
    startActivity(intent);
    // Use 0, 0 to prevent flickering during transitions in a bottom nav
    overridePendingTransition(0, 0);
    return true;
    }

    return false;
    });
    }
    }